fragment ResourcePoolFields on ResourcePool {
    id
    name
    endpoint
    contentLibraryName
    insecure
    syncStatus
    lastSyncedAt
    createdAt
    updatedAt
  }

  query ResourcePool($id: ID!) {
    resourcePool(id: $id) {
      ...ResourcePoolFields
    }
  }
