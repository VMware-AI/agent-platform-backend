query AuditLogs($filter: AuditFilter, $page: PageInput) {
    auditLogs(filter: $filter, page: $page) {
      items {
        id
        actorUserId
        actorName
        action
        resourceType
        resourceId
        ip
        result
        detail
        createdAt
      }
      total
    }
  }
