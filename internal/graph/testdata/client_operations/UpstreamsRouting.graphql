query UpstreamsRouting {
    upstreams {
      ...UpstreamRoutingFields
    }
  }
  
  fragment UpstreamRoutingFields on Upstream {
    id
    name
    provider
    apiBase
    model
    enabled
    createdAt
  }
