// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/VMware-AI/agent-platform-backend/ent/routertier"
	"github.com/google/uuid"
)

// RouterTier is the model entity for the RouterTier schema.
type RouterTier struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Tier holds the value of the "tier" field.
	Tier routertier.Tier `json:"tier,omitempty"`
	// ModelAlias holds the value of the "model_alias" field.
	ModelAlias   string `json:"model_alias,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RouterTier) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case routertier.FieldTier, routertier.FieldModelAlias:
			values[i] = new(sql.NullString)
		case routertier.FieldCreatedAt, routertier.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case routertier.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RouterTier fields.
func (_m *RouterTier) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case routertier.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case routertier.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case routertier.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case routertier.FieldTier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tier", values[i])
			} else if value.Valid {
				_m.Tier = routertier.Tier(value.String)
			}
		case routertier.FieldModelAlias:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field model_alias", values[i])
			} else if value.Valid {
				_m.ModelAlias = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RouterTier.
// This includes values selected through modifiers, order, etc.
func (_m *RouterTier) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RouterTier.
// Note that you need to call RouterTier.Unwrap() before calling this method if this RouterTier
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RouterTier) Update() *RouterTierUpdateOne {
	return NewRouterTierClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RouterTier entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RouterTier) Unwrap() *RouterTier {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RouterTier is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RouterTier) String() string {
	var builder strings.Builder
	builder.WriteString("RouterTier(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("tier=")
	builder.WriteString(fmt.Sprintf("%v", _m.Tier))
	builder.WriteString(", ")
	builder.WriteString("model_alias=")
	builder.WriteString(_m.ModelAlias)
	builder.WriteByte(')')
	return builder.String()
}

// RouterTiers is a parsable slice of RouterTier.
type RouterTiers []*RouterTier
