package graph

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.91

import (
	"context"

	"github.com/VMware-AI/agent-platform-backend/internal/graph/model"
)

// DashboardOverview assembles the console overview page from REAL entities: stat
// counts (agents by status, virtual keys, gateways, resource pools, users), the
// current month's calls/tokens/cost (over TokenUsage), the newest agents, and
// system notices derived from the most recent audit logs. Every figure is a live
// DB read — no mock data. (Helpers live in dashboard_helpers.go so gqlgen's
// resolver-file rewriter doesn't strip them.)
func (r *queryResolver) DashboardOverview(ctx context.Context, recentLimit *int, noticeLimit *int) (*model.DashboardOverview, error) {
	stats, err := r.dashboardStats(ctx)
	if err != nil {
		return nil, err
	}
	recent, err := r.dashboardRecentAgents(ctx, clampLimit(recentLimit, defaultDashboardLimit, maxDashboardLimit))
	if err != nil {
		return nil, err
	}
	notices, err := r.dashboardNotices(ctx, clampLimit(noticeLimit, defaultDashboardLimit, maxDashboardLimit))
	if err != nil {
		return nil, err
	}
	return &model.DashboardOverview{Stats: stats, RecentAgents: recent, Notices: notices}, nil
}
