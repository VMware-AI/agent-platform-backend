query VirtualKeys($userId: ID) {
    virtualKeys(userId: $userId) {
      ...VirtualKeyFields
    }
  }
  
  fragment VirtualKeyFields on VirtualKey {
    id
    alias
    userId
    agentId
    status
    expiresAt
    createdAt
  }
