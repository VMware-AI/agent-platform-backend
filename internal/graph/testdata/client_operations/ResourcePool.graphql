fragment ResourcePoolFields on ResourcePool {
    id
    name
    endpoint
    connectionStatus
    datacenterCount
    clusterCount
    esxiHostCount
    vmInstanceCount
    syncStatus
    lastSyncedAt
    createdAt
    updatedAt
  }

  query ResourcePool($id: ID!) {
    resourcePool(id: $id) {
      ...ResourcePoolFields
    }
  }
