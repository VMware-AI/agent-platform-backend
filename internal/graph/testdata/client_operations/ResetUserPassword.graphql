fragment AccountUserFields on AccountUser {
    id
    username
    displayName
    email
    role {
      id
      name
    }
    connectionStatus
    lastLoginAt
    enabled
    createdAt
    updatedAt
  }

  mutation ResetUserPassword($id: ID!) {
    resetUserPassword(id: $id) {
      user {
        ...AccountUserFields
      }
      generatedPassword
    }
  }
